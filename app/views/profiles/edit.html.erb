<%= @user.first_name %>
<%= simple_form_for @user, url: profile_path, method: :patch do |f| %>
  <%= f.input :first_name, label: 'First Name' %>
  <%= f.input :last_name, label: 'Last Name' %>
  <%= f.submit "Update⚙️", class: 'btn btn-success btn-sm' %>
<% end %>