<div class="mt-3">
</div>
<% @users_pending.each do |user| %>
  <div class="card mb-3" >
    <div class="row g-0">
      <div class="col-md-2">
        <% if user.avatar.attached? %>
            <%= image_tag user.avatar, :style => "height: 200px; width: 200px;", :gravity => "face", :class => "rounded-circle p-2" %>
          <%# elsif %>
            <%#= image_tag current_user.linkedin_picture_url, class: "rounded-circle p-2", style: "height: 100%; width: 200px;" %>
          <% else %>
            <%= image_tag "https://res.cloudinary.com/numama/image/upload/v1616795855/DISCOSPORT/discop_visual_boxing1_lwsyrs.png" , :style => "height: 100%; width: 200px;" %>
        <% end %>
      </div>
      <div class="col-md-6">
        <div class="card-body">
          <!-- <h5 class="card-title text-uppercase"><%= user.first_name %> <%= user.last_name %></h5> -->
          <h5 class="card-title text-uppercase"><%= link_to "#{user.full_name}", chatroom_path(user) %></h5>
          <h6 class="card-text"> <span class="font-weight-bold"><%= user.company %></span>  from <span class="font-weight-bold"><%= user.country %></span>  and attending as <span class="font-weight-bold"><%= user.member %></span></h6>
          <p class="card-text"> <span class="font-weight-bold"><i class="far fa-envelope"> </i> <%= user.email %></span>  | <i class="fab fa-whatsapp"></i>  <span class="font-weight-bold"><%= user.whatsapp %></span> </p>
          <div class="d-flex">
            <%= link_to "linkedin", user.linkedin, :target => '_blank', :class => "btn btn-sm btn-outline-primary text-uppercase m-2" %>
            <%= link_to "WEBSITE", "http://#{user.website}" , :class => "btn btn-sm btn-outline-primary text-uppercase m-2", :target => '_blank' %>
            <%= link_to "Delegate Profile", profile_path(user), class: " btn btn-sm btn-outline-primary text-uppercase m-2" %>
          </div>
          <p class="card-text"><small class="text-muted"><%= user.created_at %></small></p>
        </div>
      </div>
      <div class="col-md-4 align-self-center ">
        <div class="d-flex justify-content-center">
          <h5 class="card-title text-uppercase text-danger ">Status : <%= user.status %></h5>
        </div>
        <%#= link_to user.status, edit_profile_path(user), class: "btn btn-lg btn-outline-danger text-uppercase justify-content-right" %>
        <div class="d-flex justify-content-center">
          <%= simple_form_for user, url: profile_path(user), method: :patch do |f| %>
            <%= f.hidden_field :status, value: 'approved' %>
            <%= f.submit 'Approve', class: 'btn btn-success m-1' %>
          <% end %>
          <%= simple_form_for user, url: profile_path(user), method: :patch do |f| %>
            <%= f.hidden_field :status, value: 'not approved' %>
            <%= f.submit 'Disapprove', class: 'btn btn-danger m-1' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% end %>



<% @users_approved.each do |user| %>
  <div class="card mb-3" >
    <div class="row g-0">
      <div class="col-md-2">
        <% if user.avatar.attached? %>
          <%= image_tag user.avatar, :style => "height: 100%; width: 200px;", :gravity => "face", :class => "rounded-circle p-2" %>
            <%# elsif %>
              <%#= image_tag current_user.linkedin_picture_url, class: "rounded-circle p-2", style: "height: 100%; width: 200px;" %>
            <% else %>
              <%= image_tag "https://res.cloudinary.com/numama/image/upload/v1616795855/DISCOSPORT/discop_visual_boxing1_lwsyrs.png" , :style => "height: 100%; width: 200px;"%>
        <% end %>
      </div>
      <div class="col-md-7">
        <div class="card-body">
          <!-- <h5 class="card-title text-uppercase"><%= user.first_name %> <%= user.last_name %></h5> -->
          <h5 class="card-title text-uppercase"><%= link_to "#{user.full_name}", chatroom_path(user) %></h5>
          <h6 class="card-text"> <span class="font-weight-bold"><%= user.company %></span>  from <span class="font-weight-bold"><%= user.country %></span>  and attending as <span class="font-weight-bold"><%= user.member %></span></h6>
          <p class="card-text"> <span class="font-weight-bold"><i class="far fa-envelope"> </i> <%= user.email %></span>  | <i class="fab fa-whatsapp"></i>  <span class="font-weight-bold"><%= user.whatsapp %></span> </p>
          <div class="d-flex">
            <%= link_to "linkedin", user.linkedin, :target => '_blank', :class => "btn btn-sm btn-outline-primary text-uppercase m-2" %>
            <%= link_to "WEBSITE", "http://#{user.website}" , :class => "btn btn-sm btn-outline-primary text-uppercase m-2", :target => '_blank' %>
            <%= link_to "Delegate Profile", profile_path(user), class: " btn btn-sm btn-outline-primary text-uppercase m-2" %>
          </div>
          <p class="card-text"><small class="text-muted"><%#= user.created_at %></small></p>
        </div>
      </div>
      <div class="col-md-3 align-self-center ">
        <div class="d-flex flex-column">
          <div class="d-flex justify-content-center">
            <h5 class="card-title text-uppercase text-primary ">Status : <%= user.status %></h5>
          </div>


          <div class="d-flex justify-content-center">
            <%= simple_form_for user, url: profile_path(user), method: :patch do |f| %>
              <%= f.hidden_field :status, value: 'not approved' %>
              <%= f.submit "disapprove", class: 'btn btn-outline-danger text-uppercase' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>




<% @users_not_approved.each do |user| %>
  <div class="card mb-3" >
    <div class="row g-0">
      <div class="col-md-2">
        <% if user.avatar.attached? %>
          <%= image_tag user.avatar, :style => "height: 100%; width: 200px;", :gravity => "face", :class => "rounded-circle p-2" %>
          <% elsif %>
            <%= image_tag current_user.linkedin_picture_url, class: "rounded-circle p-2", style: "height: 100%; width: 200px;" %>
          <% else %>
          <%= image_tag "https://res.cloudinary.com/numama/image/upload/v1616795855/DISCOSPORT/discop_visual_boxing1_lwsyrs.png" , :style => "height: 100%; width: 200px;" %>
        <% end %>
      </div>
      <div class="col-md-7">
        <div class="card-body">
          <!-- <h5 class="card-title text-uppercase"><%= user.first_name %> <%= user.last_name %></h5> -->
          <h5 class="card-title text-uppercase"><%= link_to "#{user.full_name}", chatroom_path(user) %></h5>
          <h6 class="card-text"> <span class="font-weight-bold"><%= user.company %></span>  from <span class="font-weight-bold"><%= user.country %></span>  and attending as <span class="font-weight-bold"><%= user.member %></span></h6>
          <p class="card-text"> <span class="font-weight-bold"><i class="far fa-envelope"> </i> <%= user.email %></span>  | <i class="fab fa-whatsapp"></i>  <span class="font-weight-bold"><%= user.whatsapp %></span> </p>
          <div class="d-flex">
            <%= link_to "linkedin", user.linkedin, :target => '_blank', :class => "btn btn-sm btn-outline-primary text-uppercase m-2" %>
            <%= link_to "WEBSITE", "http://#{user.website}" , :class => "btn btn-sm btn-outline-primary text-uppercase m-2", :target => '_blank' %>
            <%= link_to "Delegate Profile", root_path, class: " btn btn-sm btn-outline-primary text-uppercase m-2" %>
          </div>
          <p class="card-text"><small class="text-muted"><%= user.created_at %></small></p>
        </div>
      </div>
      <div class="col-md-3 align-self-center ">
        <div class="d-flex flex-column">
          <div class="d-flex justify-content-center">
            <h5 class="card-title text-uppercase text-danger ">Status : <%= user.status %></h5>
          </div>
          <div class="d-flex justify-content-center">
            <%= simple_form_for user, url: profile_path(user), method: :patch do |f| %>
              <%= f.hidden_field :status, value: 'approved' %>
              <%= f.submit 'approved', class: 'btn btn-outline-primary text-uppercase' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
